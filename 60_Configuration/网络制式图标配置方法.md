---
doc_type: config
domain: Configuration
status: active
quality: imported_reference
feature: NetworkIcon
layer: AP/Framework/SystemUI/CarrierConfig
---

# 网络制式图标配置方法

## 阅读入口

这篇记录网络制式图标客制化入口。它只回答 4G/5G/NR 图标显示由哪里控制；真实注册态、RAT 和 NR availability 判断仍回到注册流程和平台 log。

## 常见入口

| 平台/层级 | 入口 | 用途 |
|---|---|---|
| Android Framework | `MobileMappings.java` | 把网络状态和 CarrierConfig 映射到显示图标 |
| CarrierConfig | `5g_icon_configuration_string` | 5G 状态到图标名的映射 |
| CarrierConfig | `carrier_nr_availabilities_int_array` | 运营商支持 NSA/SA NR 可用性 |
| SystemUI | Mobile signal controller / icon group | 最终状态栏图标显示 |

## 定位边界

| 现象 | 先确认 |
|---|---|
| 状态栏不显示 5G | NR availability、RAT、`5g_icon_configuration_string`、SystemUI 映射 |
| 注册 5G 但图标不符 | Framework 网络状态和 CarrierConfig 映射是否一致 |
| 不同运营商图标不同 | MCC/MNC carrier config 是否命中目标配置 |
| 飞行模式后图标恢复异常 | ServiceState / TelephonyDisplayInfo 是否正确刷新 |

## 验证建议

1. 先确认 modem/AP 上报的真实 RAT 和 NR 状态。
2. 再看 `dumpsys carrier_config` 中 5G icon 相关 key。
3. 最后看 `MobileMappings.java` 和 SystemUI 日志如何选择 icon group。

## 来源记录

- [不同网络制式图标客制化](http://192.168.3.94:8888/doc/5lin5zcm572r57uc5yi25byp5zu5qch5a6i5yi25yyw-bheCydwUAs) (`bheCydwUAs`)

